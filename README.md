# checkhost-telegram-bot
Distributed uptime &amp; latency monitoring using [check-host.net] with a Telegram bot frontend, multi-node checks (HTTP / Ping / TCP), anomaly detection (baseline modeling for RTT / time / loss), and rich alerts including problematic node details.
